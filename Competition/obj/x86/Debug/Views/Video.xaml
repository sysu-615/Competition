<Page x:ConnectionId='1'
    x:Class="Competition.Views.Video"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Competition.SliderConverter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Page.Resources>
        <local:Converter x:Key="converter"/>
    </Page.Resources>


    <Grid x:ConnectionId='2' x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Media Player" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"  Style="{StaticResource Header}"/>
        <MediaPlayerElement x:ConnectionId='3' Grid.Row="1" x:Name="mediaPlayerElement" AreTransportControlsEnabled="False" HorizontalAlignment="Stretch" />
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>
            <Slider x:ConnectionId='10' Grid.Column="0" x:Name="timeSlider" Padding="10 0 10 0" StepFrequency="1" VerticalAlignment="Center"                                                                                                    />
            <TextBlock x:ConnectionId='11' Grid.Column="1"  Foreground="White" x:Name="time" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Grid>
        <RelativePanel Grid.Row="3">
            <AppBarButton x:ConnectionId='4' x:Name="PlayButton" Icon="Play" Label="Play"                         />
            <AppBarButton x:ConnectionId='5' x:Name="PauseButton" Icon="Pause" Label="Pause"                          />
            <AppBarButton x:ConnectionId='6' x:Name="StopButton" Icon="Stop" Label="Stop"                         />
            <AppBarButton x:ConnectionId='7' x:Name="FullButton" Icon="FullScreen" Label="FullScreen"                         />
            <AppBarButton x:ConnectionId='8' x:Name="VolumnButton" Icon="Volume" Label="Volumn">
                <AppBarButton.Flyout>
                    <Flyout>
                        <Slider x:ConnectionId='9' Minimum="0" Maximum="1" x:Name="Volumn" Orientation="Vertical" Width="30" Height="100" Value="0.5"  StepFrequency="0.1"                                    HorizontalAlignment="Center" />
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>
        </RelativePanel>
    </Grid>
    
</Page>

