<Page x:ConnectionId='1'
    x:Class="Competition.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Competition"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <RelativePanel Grid.Row="0" Style="{StaticResource TopBar}">
                <!-- Menu -->
                <AppBarButton x:ConnectionId='12' Icon="GlobalNavigationButton"
                                           
                        Width="48"
                        RelativePanel.AlignLeftWithPanel="true"
                        RelativePanel.AlignVerticalCenterWithPanel="True"
                        Background="Transparent"
                        Foreground="White"
                        Margin="-2 0 0 -5">
                </AppBarButton>
                <TextBlock Text="赛事系统" Style="{StaticResource Header}" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignVerticalCenterWithPanel="True"/>
                <AppBarButton x:ConnectionId='13' x:Name="LogInLogOut" Width="48" Icon="Contact" RelativePanel.AlignVerticalCenterWithPanel="True" RelativePanel.AlignRightWithPanel="True" Foreground="White">
                    <AppBarButton.Flyout>
                        <MenuFlyout>
                            <MenuFlyoutItem x:ConnectionId='14' Text="登录"                               />
                            <MenuFlyoutItem x:ConnectionId='15' Text="退出"                               />
                        </MenuFlyout>
                    </AppBarButton.Flyout>
                </AppBarButton>
            </RelativePanel>
            <SplitView x:ConnectionId='2' Grid.Row="1" Name="menuView" DisplayMode="CompactInline"  CompactPaneLength="48" OpenPaneLength="170">
                <SplitView.Pane>
                    <ScrollViewer>
                        <Grid Style="{StaticResource LeftBar}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0">

                                <!-- 一级菜单 -->
                                <ListView x:ConnectionId='4' x:Name="NavMenuPrimaryListView" 
                                         SelectionMode="None" IsItemClickEnabled="True" 
                                         ItemTemplate="{StaticResource MenuItemDataTemplate}" 
                                                                                                
                                         ItemContainerStyle="{StaticResource NavMenuItemContainerStyle}"
                                                                              />

                                <!-- Tennis二级菜单 -->
                                <ListView x:ConnectionId='5' Name="NavMenuSecondaryTennisListView"
                                         Margin="10 0 0 0"
                                         Visibility="Collapsed"
                                         SelectionMode="None" IsItemClickEnabled="True" 
                                         ItemTemplate="{StaticResource MenuItemDataTemplate}" 
                                                                                                        
                                         ItemContainerStyle="{StaticResource NavMenuItemContainerStyle}"    
                                                                                 x:FieldModifier="Public"/>

                                <!-- Tennis三级菜单 -->
                                <ListView x:ConnectionId='6' x:Name="TennisInfoListView" 
                                         Margin="20 0 0 0"
                                         Visibility="Collapsed"
                                         SelectionMode="None" IsItemClickEnabled="True" 
                                         ItemTemplate="{StaticResource MenuItemDataTemplate}" 
                                                                                            
                                         ItemContainerStyle="{StaticResource NavMenuItemContainerStyle}"
                                                                                 />

                                <!-- Badminton二级菜单-->
                                <ListView x:ConnectionId='7' x:Name="NavMenuSecondaryBadmintonListView" 
                                         Margin="10 0 0 0"
                                         Visibility="Collapsed"
                                         SelectionMode="None" IsItemClickEnabled="True" 
                                         ItemTemplate="{StaticResource MenuItemDataTemplate}" 
                                                                                                           
                                         ItemContainerStyle="{StaticResource NavMenuItemContainerStyle}"    
                                                                                 x:FieldModifier="Public" />

                                <!-- Badminton三级菜单 -->
                                <ListView x:ConnectionId='8' x:Name="BadmintonInfoListView" 
                                         Margin="20 0 0 0"
                                         Visibility="Collapsed"
                                         SelectionMode="None" IsItemClickEnabled="True" 
                                         ItemTemplate="{StaticResource MenuItemDataTemplate}"
                                                                                               
                                         ItemContainerStyle="{StaticResource NavMenuItemContainerStyle}"
                                                                                    />

                                <!-- PingPang二级菜单-->
                                <ListView x:ConnectionId='9' x:Name="NavMenuSecondaryPingPangListView" 
                                         Margin="10 0 0 0"
                                         Visibility="Collapsed"
                                         SelectionMode="None" IsItemClickEnabled="True" 
                                         ItemTemplate="{StaticResource MenuItemDataTemplate}" 
                                                                                                          
                                         ItemContainerStyle="{StaticResource NavMenuItemContainerStyle}"    
                                                                                 x:FieldModifier="Public" />

                                <!-- PingPang三级菜单 -->
                                <ListView x:ConnectionId='10' x:Name="PingPangInfoListView" 
                                         Margin="20 0 0 0"
                                         Visibility="Collapsed"
                                         SelectionMode="None" IsItemClickEnabled="True" 
                                         ItemTemplate="{StaticResource MenuItemDataTemplate}" 
                                                                                              
                                         ItemContainerStyle="{StaticResource NavMenuItemContainerStyle}"
                                                                                   />

                            </StackPanel>

                            <!--底部菜单-->
                            <ListView x:ConnectionId='3' x:Name="NavMenuBottomListView" 
                                     Grid.Row="1"
                                     SelectionMode="None" IsItemClickEnabled="True" 
                                     ItemTemplate="{StaticResource MenuItemDataTemplate}" 
                                                                                           
                                     ItemContainerStyle="{StaticResource NavMenuItemContainerStyle}" 
                                     BorderBrush="{ThemeResource SystemControlBackgroundAccentBrush}" BorderThickness="0,1,0,0" 
                                                                          />
                        </Grid>
                    </ScrollViewer>
                </SplitView.Pane>
                <Border>
                    <ScrollViewer>
                        <Frame x:ConnectionId='11' Name="ContentFrame" />
                    </ScrollViewer>
                </Border>
            </SplitView>
        </Grid>
    </Grid>
</Page>

